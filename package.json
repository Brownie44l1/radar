{
  "name": "radar",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd radar-server dev",
    "start": "bun run --cwd radar-server start",
    "build": "bun run --cwd radar-app build",
    "test": "bun run --cwd radar-server test"
  }
}
